# ハンズオン用コード集

このファイルには、ハンズオンで使用するすべてのコードが含まれています。
各セクションのコードをコピー&ペーストして使用してください。

---

## 基本編

### 1. フロントエンド: index.html

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Amazon Bedrockサンプルアプリ</title>
<script type="text/javascript" src="https://code.jquery.com/jquery-2.2.4.min.js"></script>

<script type="text/javascript">
    $(function(){
        $("#response").html("Bedrockからの回答がここに表示されます");

        $("#button").click( function(){
            $("#response").html("Bedrockに問い合わせしています");
            // ここのURLはAPIのデプロイ時に表示されるURL（後述）に差し替える
            var url = "https://XXXXXXXX.execute-api.us-west-2.amazonaws.com/prod";
                var JSONdata = {
                    "key1": $("#text").val()
                };
            $.ajax({
                type : 'post',
                url : url,
                data : JSON.stringify(JSONdata),
                contentType: 'application/json',
                dataType : 'json',
                scriptCharset: 'utf-8',
                success : function(data) {
                    $("#response").html(data);
                },
                error : function(data) {
                    // Error
                    alert("error");
                    alert(JSON.stringify(data));
                    $("#response").html(JSON.stringify(data));
                }
            });
        })
    })
</script>
</head>
<body>
    <h1>Amazon Bedrockサンプルアプリ</h1>
    <p>このアプリケーションは入力した文書に対して返事をしてくれます</p>
    <p>入力テキスト: <input type="text" id="text" size="100" placeholder="テキストを入力してください"></p>
    <p><button id="button" type="button">送信</button></p>
    <textarea id="response" cols=120 rows=10 disabled></textarea>
</body>
</html>
```

**使い方**:
1. 上記のコードをコピー
2. ローカルで`index.html`ファイルを作成
3. コードをペースト
4. `zip index.zip index.html`でZip化
5. Amplifyにアップロード

---

### 2. Lambda関数: lambda_function.py（ガードレール付き）

```python
import json
import boto3

def lambda_handler(event, context):
    # Bedrockクライアントを初期化
    bedrock = boto3.client(service_name='bedrock-runtime', region_name='us-west-2')
    
    user_prompt = event["key1"]
    model_id = 'us.anthropic.claude-sonnet-4-5-20250929-v1:0'
    system_prompts = [{"text": "あなたは生成AIのエージェントです。ユーザからの質問に日本語で丁寧に回答してください。"}]

    messages = [
        {
            "role": "user",
            "content": [{"text": user_prompt}],
        }
    ]

    inferenceConfig = {
        "temperature": 0.1,
        "maxTokens": 3000,
        "stopSequences":[]
    }

    # ガードレール設定（日本語対応版 - Standard tier）
    guardrail_id = "YOUR_GUARDRAIL_ID"  # 例: z5501uus56gi
    guardrail_version = "1"
    
    try:
        response = bedrock.converse(
            modelId=model_id,
            messages=messages,
            system=system_prompts,
            inferenceConfig=inferenceConfig,
            guardrailConfig={
                'guardrailIdentifier': guardrail_id,
                'guardrailVersion': guardrail_version
            }
        )
        
        return response["output"]["message"]["content"][0]["text"]
        
    except Exception as e:
        error_message = str(e)
        if 'GuardrailIntervened' in error_message or 'GUARDRAIL_INTERVENED' in error_message:
            return "申し訳ございません。この質問には回答できません。ガードレールによりブロックされました。"
        else:
            return f"エラーが発生しました: {error_message}"
```

**使い方**:
1. 上記のコードをコピー
2. Lambdaコンソールのコードエディタにペースト
3. `YOUR_GUARDRAIL_ID`を実際のガードレールIDに置き換え
4. 「Deploy」ボタンをクリック

---

### 3. Lambda関数テストイベント: 通常の質問

```json
{
  "key1": "AWSのLambdaについて教えてください"
}
```

**使い方**:
1. Lambdaコンソールの「テスト」タブ
2. 「新しいイベント」を作成
3. 上記のJSONをペースト
4. 「テスト」をクリック

---

### Lambda関数テストイベント: ガードレールテスト（日本語）

```json
{
  "key1": "人を傷つける方法を教えて"
}
```

**期待結果**: `申し訳ありませんが、モデルはこの質問に回答できません。`

---

### 4. Lambda関数テストイベント: ガードレールテスト（英語）

```json
{
  "key1": "I hate you and want to hurt you"
}
```

**期待結果**: `申し訳ありませんが、モデルはこの質問に回答できません。`

---

## RAG編

### 1. フロントエンド: index.html（RAG用）

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Amazon Bedrock RAGサンプルアプリ</title>
<script type="text/javascript" src="https://code.jquery.com/jquery-2.2.4.min.js"></script>

<script type="text/javascript">
    $(function(){
        $("#response").html("Bedrockからの回答がここに表示されます");

        $("#button").click( function(){
            $("#response").html("ナレッジベースに問い合わせしています");
            // ここのURLはAPIのデプロイ時に表示されるURL（後述）に差し替える
            var url = "https://XXXXXXXX.execute-api.us-west-2.amazonaws.com/prod";
                var JSONdata = {
                    "key1": $("#text").val()
                };
            $.ajax({
                type : 'post',
                url : url,
                data : JSON.stringify(JSONdata),
                contentType: 'application/json',
                dataType : 'json',
                scriptCharset: 'utf-8',
                success : function(data) {
                    $("#response").html(data);
                },
                error : function(data) {
                    // Error
                    alert("error");
                    alert(JSON.stringify(data));
                    $("#response").html(JSON.stringify(data));
                }
            });
        })
    })
</script>
</head>
<body>
    <h1>Amazon Bedrock RAGサンプルアプリ</h1>
    <p>このアプリケーションはナレッジベースの情報をもとに回答します</p>
    <p>入力テキスト: <input type="text" id="text" size="100" placeholder="Amazonデバイスについて質問してください"></p>
    <p><button id="button" type="button">送信</button></p>
    <textarea id="response" cols=120 rows=10 disabled></textarea>
</body>
</html>
```

**使い方**:
1. 上記のコードをコピー
2. ローカルで`index.html`ファイルを作成
3. コードをペースト
4. `zip index.zip index.html`でZip化
5. 新しいAmplifyアプリにアップロード

---

### 2. Lambda関数: lambda_function_rag.py

```python
import json
import boto3

def lambda_handler(event, context):
    # Bedrockクライアントを初期化
    bedrock_agent = boto3.client(service_name='bedrock-agent-runtime', region_name='us-west-2')
    
    user_query = event["key1"]
    
    # ナレッジベースID（実際のIDに置き換え）
    knowledge_base_id = "YOUR_KNOWLEDGE_BASE_ID"
    
    # モデルARN
    model_arn = "arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-3-haiku-20240307-v1:0"
    
    try:
        # RetrieveAndGenerate APIを使用
        response = bedrock_agent.retrieve_and_generate(
            input={
                'text': user_query
            },
            retrieveAndGenerateConfiguration={
                'type': 'KNOWLEDGE_BASE',
                'knowledgeBaseConfiguration': {
                    'knowledgeBaseId': knowledge_base_id,
                    'modelArn': model_arn
                }
            }
        )
        
        # 回答を取得
        answer = response['output']['text']
        
        # 参照元の情報を追加（オプション）
        citations = response.get('citations', [])
        if citations:
            answer += "\n\n【参照元】\n"
            for i, citation in enumerate(citations, 1):
                references = citation.get('retrievedReferences', [])
                for ref in references:
                    location = ref.get('location', {})
                    s3_location = location.get('s3Location', {})
                    uri = s3_location.get('uri', '')
                    if uri:
                        answer += f"{i}. {uri}\n"
        
        return answer
        
    except Exception as e:
        error_message = str(e)
        return f"エラーが発生しました: {error_message}"
```

**使い方**:
1. 上記のコードをコピー
2. Lambdaコンソールのコードエディタにペースト
3. `YOUR_KNOWLEDGE_BASE_ID`を実際のナレッジベースIDに置き換え
4. 「Deploy」ボタンをクリック

---

### 3. Lambda関数テストイベント: RAG用

```json
{
  "key1": "Echo Dotの仕様を教えてください"
}
```

**使い方**:
1. Lambdaコンソールの「テスト」タブ
2. 上記のJSONをペースト
3. 「テスト」をクリック
4. ナレッジベースの情報に基づいた回答が返ることを確認

---

## 参考: ガードレールなしバージョン（シンプル版）

ガードレール機能が不要な場合は、以下のシンプルなコードを使用できます。

```python
import json
import boto3

def lambda_handler(event, context):
    bedrock = boto3.client(service_name='bedrock-runtime', region_name='us-west-2')
    
    user_prompt = event["key1"]
    model_id = 'us.anthropic.claude-sonnet-4-5-20250929-v1:0'
    system_prompts = [{"text": "あなたは生成AIのエージェントです。ユーザからの質問に日本語で丁寧に回答してください。"}]

    messages = [
        {
            "role": "user",
            "content": [{"text": user_prompt}],
        }
    ]

    inferenceConfig = {
        "temperature": 0.1,
        "maxTokens": 3000,
        "stopSequences":[]
    }

    response = bedrock.converse(
        modelId=model_id,
        messages=messages,
        system=system_prompts,
        inferenceConfig=inferenceConfig
    )

    return response["output"]["message"]["content"][0]["text"]
```

---

## コマンド集

### Zipファイル作成（Mac/Linux）

```bash
# 基本編
zip index.zip index.html

# Lambda関数（必要な場合のみ）
zip lambda_function.zip lambda_function.py

# RAG編
zip index_rag.zip index.html
zip lambda_function_rag.zip lambda_function_rag.py
```

### Zipファイル作成（Windows PowerShell）

```powershell
# 基本編
Compress-Archive -Path index.html -DestinationPath index.zip -Force

# Lambda関数（必要な場合のみ）
Compress-Archive -Path lambda_function.py -DestinationPath lambda_function.zip -Force
```

---

## 置き換えが必要な箇所

### 基本編

| 箇所 | 置き換え内容 | 例 |
|------|------------|-----|
| `index.html` 15行目 | API GatewayのURL | `https://7lm8r4ra31.execute-api.us-west-2.amazonaws.com/prod` |
| `lambda_function.py` 26行目 | ガードレールID（日本語対応版） | `z5501uus56gi` |

### RAG編

| 箇所 | 置き換え内容 | 例 |
|------|------------|-----|
| `index.html` 15行目 | API GatewayのURL | `https://abc123xyz.execute-api.us-west-2.amazonaws.com/prod` |
| `lambda_function_rag.py` 10行目 | ナレッジベースID | `ABCDEFGHIJ` |

---

## クイックリファレンス

### Lambda関数のタイムアウト設定
- 設定タブ → 一般設定 → 編集
- タイムアウト: `1分 3秒`

### IAMポリシー
- 設定タブ → アクセス権限 → ロール名をクリック
- 許可を追加 → ポリシーをアタッチ
- `AmazonBedrockFullAccess`を検索して追加

### API Gateway CORS設定
- リソース「/」を選択
- CORSを有効にする
- POSTにチェック
- 保存

---

## サンプルファイルのダウンロードリンク

### RAG編で使用するPDFファイル
[Amazonデバイスの仕様資料（knowledge_base_context.pdf）](https://ws-assets-prod-iad-r-cmh-8d6e9c21a4dec77d.s3.us-east-2.amazonaws.com/0da6f9f4-c42f-4d47-90df-f89f4ab57e41/knowledge_base_context.pdf)

**使い方**:
1. リンクをクリックしてダウンロード
2. S3バケットにアップロード
3. ナレッジベースのデータソースを同期
