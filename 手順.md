# 基本編 - 詳細手順

## 所要時間: 1.5時間

---

## ステップ1: ガードレールの作成（日本語対応版）（10分）

### 目的
不適切なコンテンツをフィルタリングする安全装置を作成します。日本語を含む50以上の言語に対応したStandard tierを使用します。

### 手順

1. **Bedrockコンソールを開く**
   - AWSマネジメントコンソールで「Bedrock」を検索
   - リージョンが「us-west-2（オレゴン）」であることを確認

2. **ガードレールページに移動**
   - 左メニューの「構築」→「ガードレール」をクリック

3. **ガードレールを作成**
   - 「ガードレールを作成」ボタンをクリック

4. **ステップ1: ガードレールの詳細**
   - 名前: `SimpleBedrock-Guardrail-JP`
   - 説明: `日本語対応コンテンツフィルタリング`
   - ブロックメッセージ: デフォルトのまま
   - **「Cross-Region inference」を展開**
   - **「Enable cross-Region inference for your guardrail」にチェック**
   - 「次へ」をクリック

5. **ステップ2: コンテンツフィルター**
   - 「有害カテゴリのフィルターを有効にする」にチェック
   - すべてのフィルターが「高」レベルで設定されることを確認
   - **「Content filters tier」で「Standard」を選択**（重要！）
   - 「スキップして確認および作成」をクリック

6. **確認して作成**
   - Cross-Region inference: `us.guardrail.v1:0`が設定されていることを確認
   - Content filters tier: `Standard`が設定されていることを確認
   - 「ガードレールを作成」をクリック
   - 作成完了を待つ

7. **ガードレールIDをコピー**
   - 作成されたガードレールをクリック
   - 「ID」欄の値をコピー（例: `z5501uus56gi`）
   - **メモ帳などに保存しておく**

8. **バージョンを作成**
   - 「バージョンを作成」ボタンをクリック
   - 説明は空欄のまま「バージョンを作成」をクリック
   - バージョン1が作成される

---

## ステップ2: フロントエンドファイルの準備（5分）

### 手順

1. **index.htmlファイルを作成**
   - 提供されている`index.html`をダウンロード
   - または、手順書のコードをコピーして作成

2. **Zipファイルを作成**
   ```bash
   zip index.zip index.html
   ```

---

## ステップ3: AWS Amplifyへのデプロイ（10分）

### 手順

1. **Amplifyコンソールを開く**
   - 「Amplify」を検索してコンソールを開く

2. **新しいアプリを作成**
   - 「新しいアプリを作成」→「Gitなしでデプロイ」を選択

3. **アプリ情報を入力**
   - アプリ名: `Simplebedrock`
   - ブランチ名: `main`

4. **ファイルをアップロード**
   - `index.zip`をドラッグ&ドロップ
   - 「保存してデプロイ」をクリック

5. **デプロイ完了を待つ**
   - 数分待つ
   - デプロイURLをコピー（例: `https://main.d22t4m3585kik8.amplifyapp.com`）

6. **動作確認**
   - URLにアクセスして画面が表示されることを確認

---

## ステップ4: Lambda関数の作成（15分）

### 手順

1. **Lambdaコンソールを開く**
   - 「Lambda」を検索してコンソールを開く

2. **関数を作成**
   - 「関数の作成」をクリック
   - 「一から作成」を選択
   - 関数名: `SimpleBedrock`
   - ランタイム: `Python 3.14`（または最新のPython 3.x）
   - 「関数の作成」をクリック

3. **コードを更新**
   - Lambdaコンソールのコードエディタに、提供されているコードをコピー&ペースト
   - **重要**: `YOUR_GUARDRAIL_ID`を実際のガードレールIDに置き換え
   - 「Deploy」ボタンをクリック
   
   **注意**: コードが長い場合やエディタで問題がある場合は、Zipファイルでアップロードする方法もあります：
   ```bash
   # ローカルでファイルを作成
   zip lambda_function.zip lambda_function.py
   # Lambdaコンソールで「アップロード元」→「.zip ファイル」を選択
   ```

6. **タイムアウトを設定**
   - 「設定」タブ→「一般設定」→「編集」
   - タイムアウト: `1分 3秒`
   - 「保存」をクリック

7. **IAMポリシーをアタッチ**
   - 「設定」タブ→「アクセス権限」
   - ロール名をクリック（新しいタブで開く）
   - 「許可を追加」→「ポリシーをアタッチ」
   - `AmazonBedrockFullAccess`を検索して選択
   - 「許可を追加」をクリック

8. **テストイベントで動作確認**
   - 「テスト」タブをクリック
   - 「新しいイベント」を作成
   - イベントJSON:
     ```json
     {
       "key1": "こんにちは"
     }
     ```
   - 「テスト」をクリック
   - 正常に回答が返ることを確認

9. **ガードレールのテスト**
   - テストイベントを変更:
     ```json
     {
       "key1": "I hate you"
     }
     ```
   - 「テスト」をクリック
   - `申し訳ありませんが、モデルはこの質問に回答できません。`が返ることを確認

---

## ステップ5: API Gatewayの作成（15分）

### 手順

1. **API Gatewayコンソールを開く**
   - 「API Gateway」を検索

2. **REST APIを作成**
   - 「APIを作成」→「REST API」の「構築」をクリック
   - API名: `SimpleBedrockAPI`
   - 「APIを作成」をクリック

3. **POSTメソッドを作成**
   - 「メソッドを作成」をクリック
   - メソッドタイプ: `POST`
   - 統合タイプ: `Lambda 関数`
   - Lambda関数: `SimpleBedrock`（またはARNを直接入力）
   - 「メソッドを作成」をクリック

4. **CORSを有効化**
   - リソース「/」を選択
   - 「CORSを有効にする」をクリック
   - POSTにチェックが入っていることを確認
   - 「保存」をクリック

5. **APIをデプロイ**
   - 「APIをデプロイ」をクリック
   - ステージ: `新しいステージ`
   - ステージ名: `prod`
   - 「デプロイ」をクリック

6. **API URLをコピー**
   - 表示されたURLをコピー（例: `https://7lm8r4ra31.execute-api.us-west-2.amazonaws.com/prod`）

---

## ステップ6: HTMLファイルの更新と再デプロイ（10分）

### 手順

1. **index.htmlを更新**
   - 15行目のURLを、API GatewayのURLに置き換え
   ```javascript
   var url = "https://7lm8r4ra31.execute-api.us-west-2.amazonaws.com/prod";
   ```

2. **Zipファイルを再作成**
   ```bash
   zip index.zip index.html
   ```

3. **Amplifyに再デプロイ**
   - Amplifyコンソールで「Simplebedrock」アプリを開く
   - 「アップデートをデプロイ」をクリック
   - 更新した`index.zip`をアップロード
   - 「保存してデプロイ」をクリック

4. **デプロイ完了を待つ**
   - 数分待つ

---

## ステップ7: 動作確認とテスト（10分）

### テストケース

#### テスト1: 通常の質問
1. Amplify URLにアクセス
2. 入力: `AWSのLambdaについて教えてください`
3. 「送信」をクリック
4. **期待結果**: 詳しい回答が表示される

#### テスト2: ガードレールテスト
1. 入力: `I hate you and want to hurt you`
2. 「送信」をクリック
3. **期待結果**: `申し訳ありませんが、モデルはこの質問に回答できません。`

#### テスト3: 日本語での通常質問
1. 入力: `こんにちは！元気ですか？`
2. 「送信」をクリック
3. **期待結果**: 丁寧な日本語の回答が表示される

---

## トラブルシューティング

### Lambda関数が動作しない
- タイムアウト設定を確認（1分以上）
- IAMポリシー`AmazonBedrockFullAccess`が付与されているか確認
- CloudWatch Logsでエラーを確認

### API Gatewayでエラー
- CORSが有効化されているか確認
- POSTメソッドが許可されているか確認
- Lambda関数が正しく統合されているか確認

### ガードレールが動作しない
- ガードレールIDが正しいか確認
- バージョン番号が`"1"`（文字列）であることを確認
- Bedrockコンソールのテスト機能で動作確認

### すべての質問がブロックされる
- ガードレールの設定が厳しすぎる可能性
- コンテンツフィルターの閾値を「中」に調整

---

## 完了！

基本編のハンズオンが完了しました。以下の機能を持つアプリケーションが完成しています：

- ✅ Webインターフェース（Amplify）
- ✅ サーバーレスAPI（API Gateway + Lambda）
- ✅ 生成AI機能（Amazon Bedrock）
- ✅ コンテンツフィルタリング（Guardrails）

次は、RAG編に挑戦してみましょう！
